# 794 Analytics

WordPress click and pageview analytics plugin for music sites, by Porter Media.

The main plugin file lives at the root of this repo. To ship an update: bump the Version header in backlot-click-tracker.php, commit, then publish a GitHub release tagged to match (for example v4.0.1). Installed sites pick it up automatically.
